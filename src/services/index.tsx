import { login } from "./log"
import {
  createLocalStorage,
  changeLocalStorage,
  getAllLocalStorage,
} from "./storage"

export { login, createLocalStorage, changeLocalStorage, getAllLocalStorage }
